{
  "name": "@primedhealth/api",
  "version": "0.0.0",
  "private": true,
  "description": "PrimedHealth API — NestJS REST + OpenAPI. Implemented in Phase 2 M4.",
  "scripts": {
    "build": "echo '[api] placeholder — body lands in M4' && exit 0",
    "typecheck": "echo '[api] no TS yet' && exit 0"
  }
}

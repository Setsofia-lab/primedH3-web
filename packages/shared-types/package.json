{
  "name": "@primedhealth/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas + TS types shared across web, api, and worker.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "gen": "openapi-typescript src/openapi.json --output src/api-types.d.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "typescript": "^5"
  },
  "peerDependencies": {
    "zod": "^4"
  }
}
